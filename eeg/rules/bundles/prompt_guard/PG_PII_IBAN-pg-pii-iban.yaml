# =============================================================
# eeg.prompt_surface.pii_exposure.pg-pii-iban
# =============================================================
# EEG bundled rule
# Apache-2.0 — EEG
# Source: EEG bundled rules
# Severity: high  |  ASAMM: AI-06 (+AO-01)
# =============================================================
external_source:
  tool: eeg_rule_pack
  original_id: PG_PII_IBAN
  original_title: International Bank Account Number (IBAN) detected in skill package. IBANs are se
  source_version: 1.0.0
  import_date: '2026-04-21'
  upstream: ""
  license: Apache-2.0 — EEG
  notes: EEG prompt-surface rule pack
eeg_rule_uid: eeg.prompt_surface.pii_exposure.pg-pii-iban
title: International Bank Account Number (IBAN) detected in skill package. IBANs are se
description: 'International Bank Account Number (IBAN) detected in skill package. IBANs are sensitive financial identifiers
  that should not appear in skill files. Source: EEG security catalog'
severity_default: high
category: pii_exposure
audit_surface:
- instruction_file
- skill_md
asamm:
  primary: AI-06
  secondary:
  - AO-01
  mapping_confidence: category-default
  notes: Category-level default at import. Review per rule.
file_types:
- markdown
- python
- bash
- javascript
- typescript
- text
- json
- yaml
patterns:
- (?<![A-Za-z])(?:AL|AD|AT|AZ|BH|BY|BE|BA|BR|BG|CR|HR|CY|CZ|DK|DO|TL|EE|FO|FI|FR|GE|DE|GI|GR|GL|GT|HU|IS|IQ|IE|IL|IT|JO|KZ|XK|KW|LV|LB|LY|LI|LT|LU|MK|MT|MR|MU|MC|MD|ME|NL|NO|PK|PS|PL|PT|QA|RO|LC|SM|ST|SA|RS|SC|SK|SI|ES|SE|CH|TN|TR|UA|AE|GB|VA|VG)[0-9]{2}[-
  ]?[A-Z0-9]{4}[- ]?[A-Z0-9]{4}[- ]?[A-Z0-9]{4}[- ]?[A-Z0-9]{0,18}(?![A-Za-z0-9])
exclude_patterns:
- (?i)example
- (?i)test
- (?i)placeholder
- (?i)sample
- (?i)format
- (?i)IBAN
remediation: 'Remove IBAN values from skill files. Use masked representations for documentation purposes.

  '
