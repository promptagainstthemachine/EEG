# =============================================================
# eeg.prompt_surface.hardcoded_secrets.pg-secret-slack-webhook
# =============================================================
# EEG bundled rule
# Apache-2.0 — EEG
# Source: EEG bundled rules
# Severity: high  |  ASAMM: AI-06 (+AO-01)
# =============================================================
external_source:
  tool: eeg_rule_pack
  original_id: PG_SECRET_SLACK_WEBHOOK
  original_title: Slack incoming webhook URL detected. Webhook URLs can be used to post messages t
  source_version: 1.0.0
  import_date: '2026-04-21'
  upstream: ""
  license: Apache-2.0 — EEG
  notes: EEG prompt-surface rule pack
eeg_rule_uid: eeg.prompt_surface.hardcoded_secrets.pg-secret-slack-webhook
title: Slack incoming webhook URL detected. Webhook URLs can be used to post messages t
description: 'Slack incoming webhook URL detected. Webhook URLs can be used to post messages to Slack channels and should
  not be hardcoded. Source: EEG security catalog'
severity_default: high
category: hardcoded_secrets
audit_surface:
- instruction_file
- skill_md
- mcp_manifest
- plugin_manifest
asamm:
  primary: AI-06
  secondary:
  - AO-01
  mapping_confidence: category-default
  notes: Category-level default at import. Review per rule.
file_types:
- python
- bash
- markdown
- javascript
- typescript
- text
- json
- yaml
patterns:
- https://hooks\.slack\.com/services/T[A-Z0-9]{8,}/B[A-Z0-9]{8,}/[A-Za-z0-9]{24}
exclude_patterns:
- (?i)example
- (?i)placeholder
- (?i)test
remediation: 'Store webhook URLs in environment variables or a secrets manager.

  '
